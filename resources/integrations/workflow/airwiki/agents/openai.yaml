interface:
  display_name: "AirWiki"
  short_description: "Maintain durable knowledge in local AirWiki memory"
  brand_color: "#0B8394"
  default_prompt: "Use $airwiki to consult or maintain the durable knowledge relevant to this work."
dependencies:
  tools:
    - type: "mcp"
      value: "airwiki"
      description: "Local AirWiki search and application-scoped memory tools"
policy:
  allow_implicit_invocation: true
