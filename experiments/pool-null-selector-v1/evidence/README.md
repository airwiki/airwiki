# Retired experiment evidence

The one-shot runner wrote `compatibility-attempt.json` and
`compatibility-report.json` here before it was retired. These aggregate files
are committed with the rejected outcome. The experiment version is closed and
must never be rerun; a new hypothesis requires a new experiment identity and
fresh promotion evidence.

No document text, query text, passage text, individual corpus identifier, label
or per-pair score belongs in this directory.
